export class Card {
  constructor(id, suit, value, points, dealed, imageUrl) {
    this.id = id;
    this.suit = suit;
    this.value = value;
    this.points = points;
    this.dealed = dealed;
    this.imageUrl = imageUrl;
  }
}
